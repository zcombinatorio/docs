---
title: "Set Token Threshold"
description: "PUT /dao/:daoPda/proposer-threshold - Set the minimum token balance required to create proposals"
---

## Overview

Sets the minimum token holding threshold for proposal creation. Any wallet holding at least this amount of the DAO's token can create proposals, regardless of whitelist status.

<CodeGroup>

```bash curl
curl -X PUT https://api.zcombinator.io/dao/Dao1xgp...ABC/proposer-threshold \
  -H "Content-Type: application/json" \
  -H "x-wallet-address: YOUR_WALLET" \
  -H "x-signature: YOUR_SIGNATURE" \
  -H "x-timestamp: 1736789200" \
  -d '{
    "wallet": "9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM",
    "threshold": "1000000000"
  }'
```

```javascript fetch
const daoPda = 'Dao1xgp...ABC';

const response = await fetch(
  `https://api.zcombinator.io/dao/${daoPda}/proposer-threshold`,
  {
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
      'x-wallet-address': walletAddress,
      'x-signature': signature,
      'x-timestamp': timestamp
    },
    body: JSON.stringify({
      wallet: '9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM',
      threshold: '1000000000'  // 1 billion raw units
    })
  }
);

const result = await response.json();
```

```python requests
import requests

dao_pda = "Dao1xgp...ABC"

data = {
    "wallet": "9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM",
    "threshold": "1000000000"  # 1 billion raw units
}

response = requests.put(
    f'https://api.zcombinator.io/dao/{dao_pda}/proposer-threshold',
    json=data,
    headers={
        'Content-Type': 'application/json',
        'x-wallet-address': wallet_address,
        'x-signature': signature,
        'x-timestamp': timestamp
    }
)

result = response.json()
```

</CodeGroup>

## Path Parameters

<ParamField path="daoPda" type="string" required>
  The DAO's Program Derived Address
</ParamField>

## Request Parameters

<ParamField body="wallet" type="string" required>
  The DAO owner's wallet address (must match x-wallet-address header)
</ParamField>

<ParamField body="threshold" type="string">
  Minimum token balance in **raw units** (including decimals). Set to `null`, `""`, or `"0"` to disable threshold-based access.
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Whether the operation succeeded
</ResponseField>

<ResponseField name="dao_pda" type="string">
  The DAO's Program Derived Address
</ResponseField>

<ResponseField name="dao_name" type="string">
  The DAO's name
</ResponseField>

<ResponseField name="proposer_token_threshold" type="string | null">
  The new threshold value, or `null` if disabled
</ResponseField>

### Success Response

```json
{
  "success": true,
  "dao_pda": "Dao1xgp...ABC",
  "dao_name": "MyDAO",
  "proposer_token_threshold": "1000000000"
}
```

### Disabling Threshold

```json
{
  "success": true,
  "dao_pda": "Dao1xgp...ABC",
  "dao_name": "MyDAO",
  "proposer_token_threshold": null
}
```

## Error Responses

<AccordionGroup>
  <Accordion title="400 - Invalid DAO PDA">
    ```json
    {
      "error": "Invalid DAO PDA"
    }
    ```
  </Accordion>

  <Accordion title="400 - Invalid Threshold">
    ```json
    {
      "error": "Threshold must be a non-negative integer string (raw token units)"
    }
    ```
  </Accordion>

  <Accordion title="403 - Not DAO Owner">
    ```json
    {
      "error": "Only the DAO owner can update the proposer threshold"
    }
    ```
  </Accordion>

  <Accordion title="404 - DAO Not Found">
    ```json
    {
      "error": "DAO not found"
    }
    ```
  </Accordion>
</AccordionGroup>

## Token Threshold Examples

<Info>
The threshold is specified in **raw token units**, not decimal amounts. For a token with 9 decimals:
</Info>

| Desired Amount | Raw Units (9 decimals) |
| :--- | :--- |
| 1 token | `"1000000000"` |
| 100 tokens | `"100000000000"` |
| 1,000 tokens | `"1000000000000"` |
| 1,000,000 tokens | `"1000000000000000"` |

## Authorization Logic

A wallet can create proposals if **either** condition is met:
1. The wallet is on the proposer whitelist, **OR**
2. The wallet holds at least `threshold` raw units of the DAO's token

Setting the threshold to `null` means only whitelisted proposers can create proposals.
